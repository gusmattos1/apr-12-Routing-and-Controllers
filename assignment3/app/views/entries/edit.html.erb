<style media="screen">
  strong {
    text-transform: uppercase;
  }
</style>


<h1>Editing: <strong><%= link_to @entry.word, entry_path(@entry.id) %></strong></h1>

<%= render 'entry_form', entry: @entry %>

<%= link_to 'Back to dictionary', entries_path %>
<br>
<br>
<%= link_to 'Delete', entry_path(@entry), method: :delete, data: {confirm: "Are you sure you want to delete the entry '#{@entry.word}'? This cannot be undone!"} %>
